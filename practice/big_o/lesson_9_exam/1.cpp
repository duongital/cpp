#include <bits/stdc++.h>

using namespace std;

int main() {
  int tc;
  cin >> tc;
  while (tc--) {
    int n, m;
    cin >> n >> m;

    stack<int> stk;
    while (n--) {
      int e;
      cin >> e;
      stk.push(e);
    }

    int result = 0;
    int top = stk.top();
    // if (top != )

    // m: is original index
  }
  return 0;
}